# 意图识别功能说明

## 功能概述

在语音转写前端界面中新增了**意图识别服务**配置功能，可以实时识别对话中的意图。

## 部署信息

- **默认服务地址**: http://188.18.18.169:8024/intent
- **服务状态**: 已运行在 188.18.18.169 服务器上
- **实现文件**: jyd_intentV1.py

## 使用方法

### 1. 配置服务地址

在右侧配置面板的**识别设置**卡片中，找到**意图识别服务**选项：

1. **输入服务地址**: 在输入框中输入意图识别服务的完整URL
   - 默认: `http://188.18.18.169:8024/intent`
   - 格式: `http://服务器IP:端口/intent`
   - 支持修改为其他服务器地址

2. **保存地址**: 输入的地址会自动保存到浏览器本地存储，刷新页面后自动恢复

### 2. 测试连接

点击 **🔍 测试连接** 按钮，系统会：
- 向配置的服务地址发送测试请求
- 显示连接状态（成功/失败）
- 检测网络延迟和服务响应

测试结果说明：
- ✓ **服务连接正常**（绿色）: 服务可用，延迟正常
- ⚠ **连接超时（5秒）**（橙色）: 服务响应慢或网络问题
- ⚠ **网络错误或跨域问题**（橙色）: 无法访问或CORS配置问题
- ⚠ **服务返回错误**（橙色）: 服务运行但返回错误

### 3. 启用意图识别

测试连接成功后，选择 **✨ 启用** 选项：
- 系统会自动再次测试连接
- 启用后每次语音识别结果都会自动调用意图识别
- 可随时切换回 **关闭** 状态

### 4. 查看识别结果

#### 服务状态提示
在配置区域下方会显示实时状态：
- ✓ 意图识别已启用（绿色）
- ✓ 服务连接正常（绿色）
- ⚠ 服务连接失败，请检查服务是否运行（橙色）
- ⚠ 请先配置服务地址（橙色）

#### 控制台输出
在浏览器控制台（F12）中可以看到详细的意图识别结果：
```javascript
🎯 意图识别结果: {
  text: "打开白板",
  intent_id: 11,
  timestamp: "14:23:45",
  latency_ms: 125.5,
  extra: { intent_id: 11 }
}
```

#### 界面显示
在转写文本后会显示一个小标签：
- 🎯 Intent 11
- 鼠标悬停可查看详细信息：
  - 意图ID
  - 识别时间
  - 原文
  - 服务延迟
  - 额外参数（如有）

### 4. 支持的识别模式

意图识别功能支持所有ASR模式：
- **2pass模式**: 对实时识别（online）和纠错结果（offline）都会调用
- **online模式**: 对实时识别结果调用
- **offline模式**: 对文件转写和离线识别结果调用

## 意图ID说明

根据 jyd_intentV1.py 中的定义，支持的意图ID包括：

### 录制相关 (1-4)
- 1: 开始录制
- 2: 暂停录制
- 3: 结束录制
- 4: 继续录制

### 回放相关 (5-10)
- 5: 打开回放模块
- 6: 暂停播放
- 7: 继续播放
- 8: 关闭回放
- 9: 快进
- 10: 回退

### 白板/批注 (11-13)
- 11: 打开白板
- 12: 打开批注
- 13: 关闭批注

### 其他功能 (14-43)
- 14: 远程互动
- 15: 打开国戏课程
- 16: 打开投屏
- 17: 关闭投屏
- 18: 退出AI
- 19: 打开U盘
- 20: 根据位置打开回放视频
- 21: 根据名字和时间打开视频
- 22: 继续（需上下文判断）
- 23: 查询录制状态
- 24: 查询录制时长
- 25: 切换回放tab
- 26: 删除回放记录
- 27: 开启AI字幕
- 28: 关闭AI字幕
- 29: 跳转至指定时间点
- 30: 调节播放速度
- 31: 调高媒体音量
- 32: 调低媒体音量
- 33: 切换"我的课程/全部课程"
- 34: 按剧种筛选
- 35: 搜索课程
- 36: 开始学习
- 37: 切换正常模式
- 38: 切换伴奏模式
- 39: 开启镜像动作
- 40: 关闭镜像动作
- 41: 台词跳转
- 42: 语音助手唤醒词
- 43: 返回上一级

## API调用详情

### 请求格式
```json
POST http://188.18.18.169:8024/intent
Content-Type: application/json

{
  "text": "打开白板"
}
```

### 响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "intent_id": 11,
    "latency_ms": 125.5
  },
  "latency_ms": 125.5
}
```

### 特殊意图返回额外字段

某些意图会返回额外的参数：

#### Intent 9/10 (快进/回退)
```json
{
  "intent_id": 9,
  "seek_time": 30,
  "unit": "SECOND"
}
```

#### Intent 29 (跳转至指定时间点)
```json
{
  "intent_id": 29,
  "minutes": 5,
  "seconds": 30
}
```

#### Intent 30 (调节播放速度)
```json
{
  "intent_id": 30,
  "speed": 1.5
}
```

#### Intent 41 (台词跳转)
```json
{
  "intent_id": 41,
  "lyric": "我家小姐桂英"
}
```

## 技术实现

### 核心功能

#### 1. 可配置服务地址
- 输入框支持自定义服务地址
- 地址保存到 `localStorage`，刷新页面自动恢复
- 支持动态切换不同的意图识别服务

#### 2. 连接测试功能
- **自动测试**: 启用服务时自动测试连接
- **手动测试**: 点击"测试连接"按钮随时测试
- **超时控制**: 5秒超时保护，避免长时间等待
- **错误分类**: 区分超时、网络错误、服务错误等不同情况

#### 3. 实时意图识别
- 异步调用，不阻塞语音识别主流程
- 8秒超时保护，避免影响用户体验
- 支持所有ASR模式（2pass/online/offline）
- 自动记录识别历史

### 代码结构

#### JavaScript 核心函数

```javascript
// 获取配置的服务地址
getIntentServiceUrl()

// 保存/加载服务地址
saveIntentServiceUrl(url)
loadIntentServiceUrl()

// 连接测试
testIntentService()              // 自动测试（启用时）
testIntentServiceManually()      // 手动测试（按钮触发）

// 意图识别
onIntentServiceChange()          // 开关变化处理
callIntentService(text)          // 调用API
displayIntentResult(record)      // 显示结果
```

#### HTML 界面元素

- `intentServiceUrl`: 服务地址输入框
- `btnTestIntent`: 测试连接按钮
- `intentServiceStatus`: 状态显示区域
- `use_intent`: 启用/关闭单选按钮

### 错误处理

#### 网络错误
- **超时错误**: 5秒（测试）/ 8秒（识别）后自动中断
- **跨域错误**: 提示"网络错误或跨域问题"
- **连接失败**: 显示具体错误信息

#### 服务错误
- **HTTP 错误**: 显示状态码（如 HTTP 500）
- **业务错误**: 显示服务返回的错误信息
- **格式错误**: 忽略无效的响应数据

#### 用户提示
- 未配置地址时自动提示并阻止启用
- 测试失败时显示具体原因
- 调用失败时仅记录日志，不影响转写

## 配置示例

### 本地开发环境
```
http://localhost:8024/intent
```

### 内网服务器
```
http://192.168.1.100:8024/intent
```

### 生产环境
```
http://188.18.18.169:8024/intent
```

### 其他端口
```
http://188.18.18.169:9000/intent
```

## 技术实现

### HTML 部分
- 在识别设置卡片中添加了意图识别服务开关
- 添加了服务地址配置输入框
- 添加了"测试连接"按钮
- 添加了服务状态显示区域
- 添加了意图标签的样式定义

### JavaScript 部分
- `getIntentServiceUrl()`: 获取配置的服务地址
- `saveIntentServiceUrl()`: 保存服务地址到 localStorage
- `loadIntentServiceUrl()`: 从 localStorage 恢复服务地址
- `onIntentServiceChange()`: 处理意图识别开关变化
- `testIntentService()`: 自动测试服务连通性
- `testIntentServiceManually()`: 手动测试连接（按钮触发）
- `callIntentService(text)`: 调用意图识别API
- `displayIntentResult(record)`: 在界面上显示意图结果
- 在 `getJsonMessage()` 中集成意图识别调用

### 数据流程
1. 用户配置服务地址
2. 点击"测试连接"或启用服务
3. 自动测试服务连通性
4. 语音识别得到文本
5. 检查意图识别是否启用
6. 如果启用，异步调用意图识别服务
7. 接收意图识别结果
8. 在控制台和界面上显示结果

## 注意事项

1. **服务地址格式**: 必须包含完整的URL（包括 http:// 或 https://）
2. **跨域配置**: 如遇CORS错误，需在Python服务端添加：
   ```python
   from flask_cors import CORS
   CORS(app)
   ```
3. **网络访问**: 前端需要能访问配置的服务器地址
4. **超时设置**: 
   - 连接测试超时: 5秒
   - 意图识别超时: 8秒
5. **地址验证**: 启用前会自动测试连接，确保服务可用
6. **自动保存**: 输入的服务地址会自动保存，无需手动保存
7. **错误容错**: 意图识别失败不会影响语音转写主流程

## 调试方法

### 1. 查看意图识别历史
在浏览器控制台输入：
```javascript
console.table(intentResultsHistory);
```

### 2. 手动测试意图识别
```javascript
callIntentService("打开白板");
```

### 3. 检查当前配置
```javascript
console.log("服务地址:", getIntentServiceUrl());
console.log("服务状态:", intentServiceEnabled);
```

### 4. 手动测试连接
```javascript
testIntentService();
```

### 5. 清除保存的配置
```javascript
localStorage.removeItem('asr_intent_service_url');
```

## 故障排查

### 问题：⚠ 服务连接失败

**可能原因：**
1. 服务未启动
2. 服务地址错误
3. 网络不通
4. 防火墙阻止

**解决方法：**
1. 检查服务是否运行：
   ```bash
   # 在服务器上检查
   ps aux | grep jyd_intentV1.py
   netstat -tulnp | grep 8024
   ```

2. 测试网络连通性：
   ```bash
   # 从浏览器所在机器测试
   curl -X POST http://188.18.18.169:8024/intent \
     -H "Content-Type: application/json" \
     -d '{"text":"你好"}'
   ```

3. 检查防火墙：
   ```bash
   # 服务器端开放端口
   sudo firewall-cmd --add-port=8024/tcp --permanent
   sudo firewall-cmd --reload
   ```

### 问题：⚠ 网络错误或跨域问题

**可能原因：**
跨域资源共享（CORS）未配置

**解决方法：**
在 `jyd_intentV1.py` 中添加 CORS 支持：

```python
from flask_cors import CORS

app = Flask(__name__)
CORS(app)  # 允许所有来源

# 或者只允许特定来源
# CORS(app, origins=["http://localhost:*", "http://192.168.*"])
```

安装依赖：
```bash
pip install flask-cors
```

### 问题：⚠ 连接超时（5秒）

**可能原因：**
1. 服务响应慢
2. 网络延迟高
3. 服务负载过高

**解决方法：**
1. 优化服务性能
2. 检查网络质量
3. 增加服务器资源

### 问题：意图识别结果不显示

**可能原因：**
1. 服务未启用
2. 识别返回了 intent_id=0（无效指令）
3. 浏览器控制台有错误

**解决方法：**
1. 确认"意图识别服务"已切换到"启用"状态
2. 打开浏览器控制台（F12）查看错误信息
3. 检查返回的 JSON 格式是否正确

## 性能优化建议

1. **批量识别**: 对于文件转写，可以收集多句后批量识别
2. **缓存结果**: 相同文本可以缓存识别结果
3. **异步处理**: 当前已采用异步方式，不阻塞主流程
4. **超时控制**: 已设置合理的超时时间，避免长时间等待
5. **错误重试**: 可以考虑添加自动重试机制（当前未实现）

## 后续优化建议

1. **意图结果可视化**: 可以添加一个专门的意图识别历史面板
2. **意图统计**: 统计各类意图的出现频率
3. **意图过滤**: 支持只显示特定类型的意图
4. **意图导出**: 支持导出意图识别历史为JSON或CSV文件
5. **置信度显示**: 如果服务端返回置信度，可以在界面上显示
6. **意图高亮**: 在转写文本中高亮触发意图的关键词

## 更新日志

### 2024-01-XX v2.0
- ✅ 添加可配置的意图识别服务地址
- ✅ 添加服务连通性测试功能（自动+手动）
- ✅ 添加详细的错误提示和分类
- ✅ 地址自动保存到 localStorage
- ✅ 增强超时控制（5秒测试/8秒识别）
- ✅ 优化用户体验和错误提示

### 2024-01-XX v1.0
- ✅ 添加意图识别服务配置开关
- ✅ 集成意图识别API调用
- ✅ 实现意图结果显示（控制台+界面标签）
- ✅ 支持所有ASR模式（2pass/online/offline）


## 后续优化建议

1. **意图结果可视化**: 可以添加一个专门的意图识别历史面板
2. **意图统计**: 统计各类意图的出现频率
3. **意图过滤**: 支持只显示特定类型的意图
4. **意图导出**: 支持导出意图识别历史为JSON或CSV文件
5. **置信度显示**: 如果服务端返回置信度，可以在界面上显示
6. **意图高亮**: 在转写文本中高亮触发意图的关键词

## 更新日志

### 2024-01-XX
- ✅ 添加意图识别服务配置开关
- ✅ 集成意图识别API调用
- ✅ 实现意图结果显示（控制台+界面标签）
- ✅ 支持所有ASR模式（2pass/online/offline）
- ✅ 添加服务连通性测试
