# 意图识别功能 - 快速开始

## 🚀 5分钟快速配置指南

### 步骤1：打开前端页面

在浏览器中打开 `index.html` 文件

### 步骤2：配置服务地址

1. 在右侧找到 **识别设置** 卡片
2. 找到 **意图识别服务** 区域
3. 在输入框中输入服务地址：
   ```
   http://188.18.18.169:8024/intent
   ```

### 步骤3：测试连接

1. 点击 **🔍 测试连接** 按钮
2. 等待2-3秒
3. 查看状态提示：
   - ✅ 绿色 = 连接成功
   - ⚠️ 橙色 = 连接失败

### 步骤4：启用服务

1. 如果测试成功，选择 **✨ 启用** 单选按钮
2. 系统会自动再次测试连接
3. 看到 "✓ 意图识别已启用" 即可

### 步骤5：开始使用

1. 连接 WebSocket 服务器
2. 点击 **▶ 开始** 按钮
3. 开始说话或上传音频文件
4. 查看识别结果和意图标签

## 📊 效果展示

### 控制台输出示例

```javascript
// 用户说："打开白板"
🎯 意图识别结果: {
  text: "打开白板",
  intent_id: 11,
  timestamp: "14:23:45",
  latency_ms: 125.5,
  extra: { intent_id: 11 }
}

// 用户说："快进30秒"
🎯 意图识别结果: {
  text: "快进30秒",
  intent_id: 9,
  timestamp: "14:24:10",
  latency_ms: 98.3,
  extra: {
    intent_id: 9,
    seek_time: 30,
    unit: "SECOND"
  }
}

// 用户说："跳到5分30秒"
🎯 意图识别结果: {
  text: "跳到5分30秒",
  intent_id: 29,
  timestamp: "14:25:20",
  latency_ms: 110.7,
  extra: {
    intent_id: 29,
    minutes: 5,
    seconds: 30
  }
}
```

### 界面显示效果

```
转写文本：打开白板 🎯 Intent 11

转写文本：快进30秒 🎯 Intent 9

转写文本：跳到5分30秒 🎯 Intent 29
```

鼠标悬停在标签上可以看到详细信息。

## ❓ 常见问题

### Q1: 测试连接失败怎么办？

**检查清单：**
- [ ] 服务器是否运行？`ps aux | grep jyd_intentV1.py`
- [ ] 端口是否开放？`netstat -tulnp | grep 8024`
- [ ] 网络是否通畅？`ping 188.18.18.169`
- [ ] 防火墙是否阻止？`sudo firewall-cmd --list-ports`

### Q2: 显示"网络错误或跨域问题"？

**解决方法：**

在服务端 `jyd_intentV1.py` 中添加：

```python
from flask_cors import CORS

app = Flask(__name__)
CORS(app)  # 添加这一行
```

然后重启服务：
```bash
pip install flask-cors
# 重启服务
```

### Q3: 意图识别结果不显示？

**检查项：**
1. 确认已启用意图识别（单选按钮选择"启用"）
2. 打开浏览器控制台（F12）查看是否有错误
3. 检查返回的 `intent_id` 是否为 0（0表示无效指令）

### Q4: 如何查看识别历史？

在浏览器控制台（F12）输入：
```javascript
console.table(intentResultsHistory);
```

### Q5: 如何更换服务器地址？

1. 直接在输入框修改地址
2. 点击"测试连接"验证新地址
3. 地址会自动保存，刷新页面后仍然生效

## 🔧 服务器端配置

### 启动意图识别服务

```bash
# 进入服务目录
cd /opt/jyd01/wangruihua/AI_Tutor

# 启动服务（后台运行）
nohup python jyd_intentV1.py > logs/8024.log 2>&1 &

# 查看日志
tail -f logs/8024.log
```

### 检查服务状态

```bash
# 检查进程
ps aux | grep jyd_intentV1.py

# 检查端口
netstat -tulnp | grep 8024

# 测试服务
curl -X POST http://localhost:8024/intent \
  -H "Content-Type: application/json" \
  -d '{"text":"打开白板"}'
```

### 开放防火墙端口

```bash
# CentOS/RHEL
sudo firewall-cmd --add-port=8024/tcp --permanent
sudo firewall-cmd --reload

# Ubuntu
sudo ufw allow 8024/tcp
sudo ufw reload
```

## 💡 使用技巧

### 技巧1：快速测试意图

在控制台直接调用：
```javascript
callIntentService("打开白板");
```

### 技巧2：批量查看历史

```javascript
// 查看所有历史
console.table(intentResultsHistory);

// 筛选特定意图
intentResultsHistory.filter(r => r.intent_id === 11);

// 统计意图分布
const counts = {};
intentResultsHistory.forEach(r => {
  counts[r.intent_id] = (counts[r.intent_id] || 0) + 1;
});
console.table(counts);
```

### 技巧3：清除历史记录

```javascript
intentResultsHistory = [];
console.log("历史已清除");
```

### 技巧4：导出识别结果

```javascript
// 复制到剪贴板
copy(JSON.stringify(intentResultsHistory, null, 2));

// 或下载为文件
const blob = new Blob([JSON.stringify(intentResultsHistory, null, 2)], {type: 'application/json'});
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = 'intent_history.json';
a.click();
```

## 📈 性能监控

### 查看平均延迟

```javascript
const avgLatency = intentResultsHistory.reduce((sum, r) => sum + r.latency_ms, 0) / intentResultsHistory.length;
console.log(`平均延迟: ${avgLatency.toFixed(2)}ms`);
```

### 查看最慢的识别

```javascript
const slowest = intentResultsHistory.sort((a, b) => b.latency_ms - a.latency_ms)[0];
console.log("最慢的识别:", slowest);
```

## 🎯 常用意图速查

| 意图ID | 说明 | 示例语句 |
|--------|------|----------|
| 1 | 开始录制 | "开始录制"、"打开录像" |
| 5 | 打开回放 | "打开回放"、"我要看回放" |
| 6 | 暂停播放 | "暂停"、"停一下" |
| 7 | 继续播放 | "继续播放"、"播放" |
| 9 | 快进 | "快进30秒"、"往后退10秒" |
| 10 | 回退 | "回退1分钟"、"往前10秒" |
| 11 | 打开白板 | "打开白板"、"我要写字" |
| 18 | 退出AI | "退下"、"再见"、"拜拜" |
| 27 | 开启字幕 | "打开字幕"、"开启字幕" |
| 29 | 跳转时间点 | "跳到5分30秒" |
| 30 | 调节速度 | "1.5倍速"、"快放" |
| 41 | 台词跳转 | "播放'我家小姐桂英'" |

完整意图列表请查看 [意图识别功能说明.md](./意图识别功能说明.md)

## 📞 技术支持

如有问题，请联系：
- 开发团队
- 查看详细文档：`意图识别功能说明.md`
- 查看服务端日志：`tail -f logs/8024.log`
